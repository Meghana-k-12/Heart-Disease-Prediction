# Heart-Disease-Prediction
Heart Disease Prediction using Machine Learning in Python. The project analyzes and visualizes the dataset using Pandas, Matplotlib, and Seaborn. Data preprocessing and feature scaling are applied, and a Random Forest classifier is used to train the model. The model predicts heart disease and is evaluated using accuracy and confusion matrix.
